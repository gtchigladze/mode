import { Theme } from './symbols';

export const lightTheme: Theme = {
  name: 'light',
  properties: {
    '--background': '#fff',
    '--on-background': '#fff',
    '--primary': '#fff',
    '--on-primary': '#000',
    '--nav-border': '#ff4d4d'
  }
};
